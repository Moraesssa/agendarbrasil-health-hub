import React from 'react';
import { AppointmentBooking } from '@/components/agendamento/AppointmentBooking';

export default function AgendamentoCompleto() {
  return (
    <div className="min-h-screen bg-background">
      <AppointmentBooking />
    </div>
  );
}