// A basic example test file

describe('Example Test Suite', () => {
  test('should return true', () => {
    expect(true).toBe(true);
  });

  test('should add two numbers', () => {
    expect(1 + 2).toBe(3);
  });
});
